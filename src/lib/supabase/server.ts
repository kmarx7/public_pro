import "server-only";import {createServerClient} from "@supabase/ssr";import {cookies} from "next/headers";export async function createClient(){const c=await cookies(),u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!u||!k)throw new Error("Supabase is not configured");return createServerClient(u,k,{cookies:{getAll:()=>c.getAll(),setAll:v=>{try{v.forEach(x=>c.set(x.name,x.value,x.options))}catch{}}}})}
